{
  "name": "pagina-sarita",
  "version": "1.0.0",
  "description": "Romantic poetry and songs collection with persistent backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node backend/save-data.js & python3 -m http.server 8080 --directory .",
    "stop": "pkill -f 'node backend/save-data.js'"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^1.4.5-lts.1"
  }
}
